# Inclua este arquivo somente no bloco `server` da API publica.
# O Express limita cada imagem a 8 MB; 30 MB comporta as tres partes e o multipart.
client_max_body_size 30m;
client_body_timeout 120s;
proxy_connect_timeout 15s;
proxy_send_timeout 120s;
proxy_read_timeout 120s;
